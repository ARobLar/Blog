

export type featuredUser = {
  username : string,
  avatarSource: string,
  avatarLabel: string,
}

export type featuredPost = {
  id : string,
  title : string,
  creationTime : Date,
  text : string,
  imageSource: string,
  imageLabel: string,
}